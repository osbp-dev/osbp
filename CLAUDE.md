# CLAUDE.md

Audience: Claude users in this repository; canonical guidance remains in AGENTS.md.

> **Project guidance lives in [AGENTS.md](AGENTS.md).** Read that file first — it covers project purpose, scope, narrow tool set, core domain objects, trust requirements, planned local shape, adapter rules, engineering conventions, and definition of done.
>
> This file exists as a pointer because Claude Code looks for `CLAUDE.md` by default. The actual rules live in `AGENTS.md` and apply to any AI agent or human working in this repo, not just Claude Code. Keeping a single source of truth eliminates the drift class where one file gets updated and the other doesn't.
>
> If you're a future tool that wants its own filename (e.g., `GEMINI.md`, `COPILOT.md`, `.cursorrules`), follow the same pattern: a thin pointer here, the canonical content in `AGENTS.md`.
